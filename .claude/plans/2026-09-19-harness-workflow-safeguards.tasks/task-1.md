# Task 1: Reconcile authorized workflow progress and check delivery size early

**Files:** Shared skills `from-issue/SKILL.md`, `from-issue/investigate.md`, `handoff/SKILL.md`, `wayfind/SKILL.md`, `design/SKILL.md`, `writing-plans/SKILL.md`, `sdd/SKILL.md`, `ship-issue/SKILL.md`, `ship-issue/HUMAN-GATE.md`; Claude skill `orchestrate-issues/SKILL.md`; owning `evals/evals.json` files. Modify existing contract tests only to remove expectations made obsolete by these requirements, never add wording-mirror tests.

**Interfaces:** Existing artifact-budget/review-package commands and fixed workflow/result JSON are unchanged. Handoff scope is prose inside the handoff artifact, not new transport keys. New behavior evals use existing prompt/expected_output structures.

**Invariants:** Specification D1–D3; no automatic grant of broader permissions, --auto mode, new attempt takeover or new terminal replay run. Existing lifecycle acquisition terminal replay remains immutable; the already-delivered reconciliation path applies to an authorized owner or ledger-free caller, not a bypass of acquisition. Unknown/competing open work is protected.

- [ ] Add plan-only eval scenarios in the owning skills. Baseline fails because from-issue investigate unconditionally stops on merged PR and asks whether this is a follow-up, checkpoints demand another approval, wayfind tells caller to start a new shipping session, and SDD measures only task ranges before the final aggregate. Preserve existing eval IDs and assertions.
- [ ] Replace the unconditional merged/open-PR stops with bounded reconciliation: verify exact issue/PR/target and acceptance evidence, resume authorized shipping or finish authorized missing bookkeeping; report what is observed and what remains unknown. If scope is different or authority absent, ask for that specific missing decision. Do not rebuild an already delivered implementation.
- [ ] Make the shared checkpoint rule honor existing explicit authorization, then remove or qualify contradictory phase clauses. Design autonomous decisions may also follow already-approved scope without pretending literal --auto was passed. Add concise handoff fields for deliverable, source/scope of authorization, next action and any actual approval denial. Wayfind finishes authorized delivery of its decision record but does not start a second decision or implementation without authorization.
- [ ] Align ship-issue permission guidance with the same authorization rule: when an existing user grant covers the concrete action, do not demand a new literal-command grant merely because this is Codex or a phase boundary. Retain the concrete reviewable gate when authority is missing and the ban on routing around an actual denial. A harmless spelling difference or transient failure does not itself erase scoped authorization; actual automatic approval decisions still apply. Keep required CI, launch guard and protected-branch restrictions. Replace obsolete exact-phrase tests only where needed and explain the behavioral contract.
- [ ] SDD setup records `DELIVERY_BASE` as the merge base with the resolved integration branch. Run existing `scripts/review-package PLAN_FILE DELIVERY_BASE HEAD` and its existing producer validation plus independent artifact check before first implementation and after each completed task, before any next dispatch. Exact base/head and metrics go to the ledger; reuse an already valid package for an identical range. Existing task-scoped review remains. Exit 2/3 cannot advance; preserve work and identify a deliverable split. Planning adds a compact estimate/risk statement and independent delivery boundaries where needed; do not promise byte-accurate forecasting for code not yet written.
- [ ] Add a concise capacity/capability guard in orchestrate-issues: known insufficient capacity cannot justify competing owners, nested relays or repeated spawn attempts; supported direct sequential work is the fallback, otherwise explicit unsupported capability. Do not calculate/override control's policy, manufacture available-slot facts or claim #150 implemented. Preserve current Claude-only entry support.
- [ ] Run existing `python3 -m unittest -q home/common/agent-skills/tests/test_workflow_skill_contracts.py home/common/agent-skills/tests/test_review_package.py` and model-matrix validation. Validate changed eval JSON/frontmatter. Parent will run independent behavior probes and full verification. Report any obsolete contract-test conflict explicitly.
- [ ] Self-review consistency and root instruction growth; commit only owned skill/eval/test paths with signed commit and `Co-authored-by: Codex <noreply@openai.com>`. Do not edit plans/specs or accounting code.
