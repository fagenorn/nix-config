[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-tools"
version = "0.1.0"
dependencies = []

[tool.setuptools.packages.find]
include = ["agent_tools*"]
