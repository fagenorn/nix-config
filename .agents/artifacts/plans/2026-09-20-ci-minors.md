# CI Contract Minors Implementation Plan

> **For agentic workers:** execute this plan with the `sdd` skill — one implementer
> per task, reviewed between tasks. Steps use `- [ ]` checkboxes.

**Goal:** Make the CI workflow and branch-protection fixture explicit, least-privileged, and exact while preserving the existing `Nix Eval` required check.

**Architecture:** Extend the repository's dependency-free Python contract test at the same textual and JSON seams it already owns. Update the workflow and complete branch-protection replacement payload only after the new assertions fail for the current state.

**Tech stack:** GitHub Actions YAML, GitHub branch-protection REST payload JSON, Python `unittest` and `re`.

## Global Constraints

- Preserve the `Nix Eval` job name, trigger behavior, evaluation command, evaluated attribute, and GitHub Actions provider.
- Grant only top-level `contents: read`; no broader or job-level permission may be introduced (per D1 and D4).
- Require exactly `Nix Eval` from GitHub Actions app ID `15368`, using `checks` with no deprecated `contexts` field (per D2).
- Keep the contract test dependency-free and return scalar contents without matching YAML quote characters (per D3).
- Do not apply remote branch protection or change action versions, billing, or administrative settings.

## Test seams

- `tests/test_branch_protection.py` is the offline public seam for workflow text and branch-protection JSON.
- Synthetic plain, single-quoted, and double-quoted job-name lines test scalar extraction.
- Exact equality pins the top-level permission map and complete protection replacement payload.
- The existing required-job assertions continue to pin evaluation semantics.

## Delivery estimate and boundaries

Estimate: one independently testable task, three modified product files, and roughly 35–60 changed lines. The package should remain far below review artifact limits; there is no independently useful partial delivery.

## Task index

Task 1 — Pin the CI and protection contracts — `.github/workflows/ci.yaml`, `.github/branch-protection.json`, `tests/test_branch_protection.py` — full — [task-1.md](2026-09-20-ci-minors.tasks/task-1.md)

## Decisions

The specification owns D1–D4.

## Standards review

- Reviewer: independent native Codex reviewer `/root/issue_39_retry/issue_39_plan_review`, requested `gpt-5.6-sol` at high effort; execution telemetry was unavailable. Reviewed against base `6d4b7a49dd3a44c079c8310e902a86665a6805f0`.
- Verdict: clean. The reviewer confirmed that D1–D4 cover all three remaining minors, preserve the `Nix Eval` contract and checkout v4, and include focused and repository-level verification.
- Historical review dispositions retained: reject all job-level `permissions` overrides (D4), and update adjacent comments that describe the legacy `contexts` payload.

---
