{
  "skill_name": "orchestrate-issues",
  "notes": "Plan-only evals. A pipeline run would dispatch real background from-issue --auto agents (hours of wall clock, background-task notifications the sandbox cannot observe), so these grade the external control-adapter discipline instead. This skill lives in the Claude-only tree (home/common/claude-code/skills/); run-eval.sh reaches it through its second skill root. Expected outputs assert role boundaries, normalized facts, bounded lifecycle interfaces, typed action execution, and compact reporting.",
  "evals": [
    {
      "id": 1,
      "name": "dispatcher-bootstrap-control-and-actions",
      "mode": "plan-only",
      "prompt": "Orchestrate issues 12, 14 and 15 on this repo. **Plan-only: dispatch nothing, run nothing.** Describe exactly what you would do from invocation through bootstrap, observation, the control call, and execution of every possible returned action. Be explicit about what you personally will and will not read, exact binding mappings, restart observations, dispatch identity, and final output.",
      "expected_output": "Role boundary: the dispatcher is an external control adapter and never reads issue content, code, specs, plans, diffs, or review findings; it does not maintain a second task ledger or assemble scheduling policy. Resolve issue numbers in caller order and bindings once, mapping resolved agentBudgetMinutes exactly to request attempt_budget_minutes and resolved maxParallel exactly to request max_parallel. Resolve one immutable ledger_repo_root and stable run_id, then call workflow-state init-run and consume only the strict version-1 bounded requirements. Each requirement's issue, attempt, owner, action_id, and recorded_worktree identifies exactly which returned path to inspect and which current host notification to correlate. Inspect every recorded_worktree; for an absent or mismatched path, and for every requested issue without a requirement, provide a distinct verified-absent candidate without classifying readiness. One tracker read supplies normalized state, open blockers, and decision blockers; owner notifications become normalized bounded events for the exact returned owner/action identity. Write the exact version-1 control request with ordered issues and normalized tracker, owner, and worktree facts, then call workflow-state control with an absolute request-file path. The response is the only source of action order, kind, and lifecycle identity. Execute its actions in returned order from the closed set spawn, resume, retry, wait, finalize; an unknown action kind is a contract error and is surfaced before that action executes. For spawn/resume/retry, pass action ID, owner token, issue, attempt, exact worktree, and optional handoff path unchanged in a background owner envelope with immutable ledger_repo_root, run_id, and literal from-issue <num> --auto; host task IDs remain correlation-only. A finalize is rendered from bounded summaries in that same response, including discussion items, without another ledger read. Failures include reading content, deciding readiness or capacity, reordering actions, changing lifecycle identity, accepting an unknown action kind, or reconstructing omitted history.",
      "files": []
    },
    {
      "id": 2,
      "name": "superseding-wait-failures-and-restart",
      "mode": "plan-only",
      "prompt": "Mid-orchestration, plan-only: explain the adapter's exact behavior for (a) the same wait action returned twice, (b) a new wait replacing a live one, (c) an old observer that is missing or already exited, (d) unexpected old-observer cancellation failure, (e) new-observer arming failure, (f) a stale wake after replacement, and (g) a full dispatcher restart. Also explain when control is called again and how finalize cleans up.",
      "expected_output": "The adapter stores only current_wait_id and current_wait_handle. The same wait ID does not install a duplicate observer. For a different ID: save the old pair, publish the new ID with no handle, cancel the old wait, then arm and store one replacement observer; never pair the new wait ID with the old handle. A missing or already-exited wait is an idempotent cancellation outcome and replacement arming continues. On unexpected cancellation failure, restore the old wait ID/handle, do not arm replacement, fail loudly, and let the next identical control response retry replacement. If arming fails after cancellation, clear both fields, report that no wake is installed, and fail loudly. Every wake carries its ID; a stale wake ID is ignored unless it equals current_wait_id. There is no polling or repeated short sleep. On full restart, the host must reap inherited detached wait observers before process-local state calls workflow-state init-run, rebuilds only bounded requirements and normalized facts, calls workflow-state control, and rearms from the returned wait action. Call control at start/resume and on each current owner notification, tracker change, or current wait-ID event. Its typed response may return spawn, resume, retry, wait, or finalize in response-only order. On finalize, clear the current wait ID before canceling the handle, tolerate a missing/already-exited handle, and render the same response's bounded summaries and discussion items. Failures include duplicate observers, treating a stale wake as current, swallowing an unexpected cancellation failure, failing to restore the old wait ID/handle, arming after that failure, claiming a wake exists after arm failure, or failing to reap inherited detached wait observers.",
      "files": []
    }
  ]
}
