{
  "skill_name": "codex-collaboration",
  "notes": "Plan-only evals. A pipeline run would need the codex-companion runtime, Codex authentication, and ~15 min of external wall clock — none of which the hermetic sandbox has — so these grade the launch/fallback/disposition procedure. This skill lives in the Claude-only tree (home/common/claude-code/skills/); run-eval.sh reaches it through its second skill root. Expected outputs assert transport and policy behavior, not SKILL.md prose; the decision-ledger expectation is marked 'contract:' because the compact row shape lands with the slimmed caller skills.",
  "evals": [
    {
      "id": 1,
      "name": "plan-review-policy-preflight-fallback",
      "mode": "plan-only",
      "prompt": "You're at from-issue Phase 5: spec and plan are committed in the worktree and it's time for the standards review pass via the `codex-collaboration` skill, operation plan-review. **Plan-only: launch nothing.** Walk me through your exact procedure, and be specific about three situations: (a) the project config sets `codex.planReview.enabled: false`; (b) `codex-companion` is not on PATH; (c) the review comes back as `CODEX_REVIEW_FAILURE: job timed out`. Also tell me what happens to the findings that do come back.",
      "expected_output": "Procedure: resolve policy from .claude/skills.config.json (missing `enabled` means true); build ONE self-contained packet that travels by paths — worktree root/branch/base SHA, issue + acceptance criteria, absolute spec and plan paths, the AGENTS.md/CLAUDE.md chain, map-first doc selection (context map, only intersecting area CONTEXT files, cited ADRs, applicable standards layers), the review contract path — then a single foreground dispatch of the bridge agent whose first line is `WORKTREE_ROOT: <absolute root>`, expecting an isolated read-only Codex runtime bounded around ~15 minutes, validated for the three sections Blocking / Should fix / Discussion. Case (a): enabled:false is a project opt-out — return control to the caller's native reviewer flow, no Codex launch at all. Case (b): the sub-second `command -v codex-companion` pre-flight catches it — capability fallback to the native flow immediately, recorded as such, never converted into a timed-out Codex attempt. Case (c): CODEX_REVIEW_FAILURE is a real Codex failure — exactly one fresh native standards reviewer gets the same packet and read-only contract (not asked to imitate Codex), the failure class is recorded, and Codex is never retried; a queued/concurrent review is never a fallback reason. Findings: the parent Claude agent re-opens every cited file and verifies each finding first, rejects stale/unsupported ones, applies verified Blocking findings to the plan (Should-fix auto-applied only in --auto; Discussion raised at the checkpoint), logs one decision entry per applied finding (contract: compact ledger row `ID | choice | grounding | rejected alternative`), adds a provenance section naming reviewer identity/base SHA/counts, and never stores the raw reviewer transcript in the repo. Failures: launching Codex despite enabled:false, retrying Codex after a failure, blending findings into one narrative without per-finding disposition, or editing files from inside the reviewer.",
      "files": []
    },
    {
      "id": 2,
      "name": "diff-review-light-packet-boundaries",
      "mode": "plan-only",
      "prompt": "sdd's final review is dispatching you for the correctness axis: operation diff-review over `abc1234..def5678` in this worktree. **Plan-only: launch nothing.** List exactly what goes into the packet you'd send, exactly what stays OUT of it and why, what shape the reviewer's output must have, and who dispositions the findings afterwards.",
      "expected_output": "IN: the operation name and directories/branch plus the base and head SHAs; a scope line limiting review to code correctness (bugs, boundary error handling, dead branches, assertions pinning the contract, DRY, cross-task integration) and explicitly telling the reviewer NOT to grade conformance — that is the parallel axis's job; the caller's correctness rubric by absolute path with placeholders filled; the diff-package path and plan path as routing context; inferred verify commands and the AGENTS.md/CLAUDE.md chain; only the standards layers matching the diff's file types. OUT — named as deliberate exclusions, not omissions: issue investigation, the spec, domain/context docs, the planReview focus, and the plan-review contract file; the light packet is what keeps Codex inside its runtime budget, and domain conformance belongs to the other axis. Output shape: first line an axis verdict (Correctness: Clean | Findings + 1-2 sentences), then exactly Critical / Important / Minor, <=400 words, every finding with stable ID, live path:line evidence, confidence, unknowns; `None.` under empty sections. Disposition: NOT this skill's job for diff-review — the validated result returns unmodified to the calling controller with the reviewer identity (Codex | Claude fallback + failure class) for its ledger; the same one-time native fallback and never-retry rules apply. Failures: reusing the plan-review packet 'plus tweaks', letting the reviewer grade spec conformance, or dispositioning findings inside this skill.",
      "files": []
    }
  ]
}
