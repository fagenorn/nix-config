---
area: System
budget: 200 lines
---

# System

The reserved area for decisions that belong to no single area — the repo-wide
constraints every other area works under. It carries no vocabulary of its own; the
records in `adr/` are the content.
