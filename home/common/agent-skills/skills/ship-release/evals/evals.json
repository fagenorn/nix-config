{
  "skill_name": "ship-release",
  "notes": "plan-only release evals for the kind=none fixture-repo. Resolve once into a retained ResolvedProject; branch, tracker, command, and deploy choices come from bindings. Refusal is fatal and policy is never defaulted or inferred.",
  "evals": [
    {"id": 1, "name": "casual-trigger-with-deploy-emphasis", "mode": "plan-only", "prompt": "Plan-only: plan a release and deploy watch. **Do not open a PR or merge anything.**", "expected_output": "Resolve once into retained ResolvedProject; use bindings.vcs, tracker, commands, workflow.release, and deploy capability. Refusal is fatal and no policy is defaulted/inferred. Preserve all six phases: changelog from origin/<default>..origin/<integration>, draft PR, CI, `gh pr merge --merge` without --delete-branch/--no-ff, and configured per-service deployment-list monitoring. Unsupported deploy is skipped only by capability state. Silent rollback is a failure unless terminal SUCCESS running commit equals merge SHA; health-200 is not proof."},
    {"id": 2, "name": "verbose-step-by-step-request", "mode": "plan-only", "prompt": "Give the detailed release sequence. **Dry-run only: narrate commands and expected outputs without creating or merging a PR.**", "expected_output": "After one ResolvedProject resolution, derive branches and commands from retained bindings; refusal is fatal, blocked stops, and no value is defaulted/inferred. Preserve the generated changelog with themed summary, included merge bullets using full PR URLs and SHAs, per-service deploy expectations, planned PR `--base <default> --head <integration>`, merge command, and deploy-watch loop. State that SUCCESS running commit must equal merge SHA and health-200 is never proof."},
    {"id": 3, "name": "specific-gotcha-emphasized", "mode": "plan-only", "prompt": "Plan-only: explain silent-rollback detection and confirm no --delete-branch.", "expected_output": "Use retained bindings; refusal is fatal and platform/branch policy is never inferred. Preserve configured deployment-list monitoring: latest terminal SUCCESS plus running commit matching merge SHA (prefix acceptable), explicitly avoid stale FAILED notification and health-200 as proof; use `gh pr merge --merge` without --delete-branch or --no-ff."},
    {"id": 4, "name": "single-branch-kind-none-local-release", "mode": "plan-only", "prompt": "Plan-only: local single-branch release; do not tag or push.", "expected_output": "One retained ResolvedProject identifies declared unsupported tracker/deploy routes; refusal is fatal and no default behavior. Preserve Phases 0 AND 1 then skip PR/CI/forge merge, continuing at 4.5. Tag local default tip with `git rev-parse <default>`, never origin; find reachable previous tag with `git tag --list 'v[0-9]*' --merged \"$MERGE_SHA\" --sort=-v:refname | head -1`; skip duplicate tags with `git tag --points-at \"$MERGE_SHA\" 'v[0-9]*'` to avoid a double-tag; local annotated tag only, no push/release; resume from `.superpowers/workflows/ship-release/state.json`; justify semver with CHANGELOG.md Version bump signals."}
  ]
}
